apiVersion: v1
kind: Service
metadata:
  name: {{ include "trust-service.fullname" . }}
  labels:
    {{- include "trust-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trust-service.selectorLabels" . | nindent 4 }}
