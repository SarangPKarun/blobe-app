apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chat-service.fullname" . }}
  labels:
    {{- include "chat-service.labels" . | nindent 4 }}
data:
  PORT: "3009"
  CORS_ORIGIN: "https://blobe.app"
