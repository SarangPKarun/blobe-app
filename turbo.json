{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".next/**", "out/**"]
    },
    "build:android": {
      "dependsOn": ["build"],
      "outputs": ["dist/**"]
    },
    "@blobe/mobile#build": {
      "dependsOn": ["@blobe/globe-webview#build:android"],
      "outputs": ["android/app/src/main/assets/globe/globe.html"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": ["coverage/**"],
      "inputs": ["src/**/*.tsx", "src/**/*.ts", "test/**/*.ts", "test/**/*.tsx"]
    },
    "lint": {
      "dependsOn": ["build"],
      "inputs": ["src/**/*.tsx", "src/**/*.ts", "src/**/*.js"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
